issueNumber: MDL-89711
notes:
  mod_quiz:
    - message: >-
        override_manager::get_effective_open_close_times() has been restored as
        a deprecated wrapper around override_manager::get_effective_times(),
        preserving its original 'timeopen'/'timeclose'-only return contract. It
        had been renamed without a backwards-compatible shim; call sites still
        using the old name should migrate to get_effective_times().
      type: deprecated
