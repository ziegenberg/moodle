issueNumber: MDL-88138
notes:
  auth_db:
    - message: >-
        The `ext_addslashes()` method has been deprecated from `auth_plugin_db`,
        because external database queries now use parameterized statements
        instead. As a result, the `sybasequoting` setting has also been removed,
        since it was only ever used by that method.
      type: deprecated
